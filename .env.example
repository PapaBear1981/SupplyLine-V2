# SupplyLine MRO Suite - Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# Flask Backend Configuration
# =============================================================================

FLASK_ENV=production
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# =============================================================================
# Security Configuration - REQUIRED
# =============================================================================
# CRITICAL: Generate strong, unique secrets for production
# Use: python -c "import secrets; print(secrets.token_urlsafe(64))"
# DO NOT use the example values below - generate your own!

SECRET_KEY=your-secure-secret-key-REPLACE-THIS
JWT_SECRET_KEY=your-jwt-secret-key-REPLACE-THIS

# Public URL for QR Codes (optional)
# Set this to your server's network IP or domain name for QR codes to work on external devices
# Examples:
#   - Local network: PUBLIC_URL=http://192.168.1.100:5000
#   - Production: PUBLIC_URL=https://yourdomain.com
# If not set, QR codes will use the request host (localhost), which won't work on phones
# PUBLIC_URL=http://192.168.1.100:5000

# =============================================================================
# CORS Configuration
# =============================================================================
# Add your frontend URLs here (comma-separated)
# Common defaults included for various frontend frameworks
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:80,http://localhost:8080

# =============================================================================
# Python Configuration
# =============================================================================

PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
TZ=UTC

# =============================================================================
# Database Configuration
# =============================================================================
# SQLite (default): Database stored in database/tools.db
# For PostgreSQL (if needed in future):
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# =============================================================================
# Database Backup Configuration (optional - defaults provided)
# =============================================================================
# Enable automatic database backups (default: true)
AUTO_BACKUP_ENABLED=true

# Backup interval in hours (default: 24)
AUTO_BACKUP_INTERVAL_HOURS=24

# Create backup on application startup (default: true)
BACKUP_ON_STARTUP=true

# Maximum number of backups to keep (default: 10)
MAX_DATABASE_BACKUPS=10

# Compress backups with gzip (default: true)
COMPRESS_BACKUPS=true

# =============================================================================
# Account Lockout Settings (optional - defaults provided)
# =============================================================================

# MAX_FAILED_ATTEMPTS=5
# INITIAL_LOCKOUT_MINUTES=15
# LOCKOUT_MULTIPLIER=2
# MAX_LOCKOUT_MINUTES=60

# =============================================================================
# Docker Port Mapping
# =============================================================================

# Backend port mapping (host:container)
BACKEND_PORT=5000

# =============================================================================
# Docker Resource Limits (optional - for Docker Swarm mode)
# =============================================================================
# CPU limits use fractional values (e.g., 0.5 = 50% of one CPU core)
# Memory limits use suffixes: M for megabytes, G for gigabytes

BACKEND_CPU_LIMIT=0.5
BACKEND_MEMORY_LIMIT=512M
